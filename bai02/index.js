let car = {
    brand: "Toyota",
    model: "Corolla",
    year: 2020,
}
console.log(car);

car.color = "red";

console.log(car);

car.year = 2022;

console.log(car);

alert(`đối tượng car với các thuộc tính sau:
    brand: ${car.brand}
    model: ${car.model}
    year: ${car.year}
    color: ${car.color}`);


